server:
  port: 8080

spring:

  datasource:
    name: ax_mall
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: jim
    password: AXing#631122
    #docker 服务
    url: jdbc:mysql://mysql:3306/ax_demo?characterEncoding=utf-8&useSSL=false&allowPublicKeyRetrieval=true

  redis:
    #    host: 47.101.156.93 # Redis服务器地址
    #docker 服务
    host: redis # Redis服务器地址
    password: AXing#631122 # Redis服务器连接密码（默认为空）
    database: 0 # Redis数据库索引（默认为0）
    port: 6379 # Redis服务器连接端口
    lettuce:
      pool:
        max-idle: 8 # 连接池中的最大空闲连接
        min-idle: 0 # 连接池中的最小空闲连接
        max-active: 8 # 连接池最大连接数（使用负值表示没有限制）
        max-wait: -1 # 连接池最大阻塞等待时间（使用负值表示没有限制）
        timeout: 5000 # 连接超时时间（毫秒）\

  devtools:
    restart:
      enabled: true
      additional-paths: src/main/java

  sql:
    init:
      mode: ALWAYS
#  quartz:
#    properties:
#      org:
#        quartz:
#          scheduler:
#            instanceName: clusteredScheduler
#            instanceId: AUTO
#          jobStore:
#            class: org.quartz.impl.jdbcjobstore.JobStoreTX
#            driverDelegateClass: org.quartz.impl.jdbcjobstore.StdJDBCDelegate
#            tablePrefix: QRTZ_
#            isClustered: false
#            clusterCheckinInterval: 10000
#            useProperties: false
#          threadPool:
#            class: org.quartz.simpl.SimpleThreadPool
#            threadCount: 10
#            threadPriority: 5
#            threadsInheritContextClassLoaderOfInitializingThread: true
#    job-store-type: jdbc

#message:
#  fileSize: 4194304
#  #文件压缩大小(大于4兆压缩)
#  upPath: /Users/axing/Desktop/UploadData/images
#  #压缩比例
#  scaleRatio: 0.20f
#  #图片类型
#  imageType: png,jpg,jpeg
t-person:
  name: jim
  age: 20
