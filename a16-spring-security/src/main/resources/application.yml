spring:
  datasource:
    url: jdbc:mysql://hadoop202:3306/ax_test16?serverTimezone=GMT%2B8&characterEncoding=utf-8&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl #开启sql日志, info级别的,也可以直接让 对应的mapper debug级别
#logging:
#  level:
#    com.github.axinger.mapper: debug

